package se.strawberry.repository.stub

/**
 * Repository boundary for stubs persisted in DB.
 */
interface StubRepository {
    // Do we need it?
    // If we need it then should we store it as it is or as WireMock's StubMapping?
}

